# Final Report Restructure Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Restructure `docs/NLP Final Report - Group 02 Section B.docx` (built by `docs/builders/build_final_report.py`) to match the group's new per-section spec: isolated tables per model family, best/average/worst analysis with reasons at every comparison point, a trimmed Sections 1/7/8, no code appendix in the docx, and a separately reorganized + PDF-exported notebook.

**Architecture:** Everything the docx contains is generated by `build_final_report.py` from JSON/parquet/npz artefacts already on disk (`experiments/paper_scale/`, `experiments/audit/`). No new experiments run — this is a rewrite of the `build_text()` prose, the table-emission blocks in `main()`, and removal of the code-appendix block. Section 4's comparison against "sir's 5 configs" is deferred (data not yet provided) and flagged inline, not fabricated.

**Tech Stack:** python-docx, pandas, numpy, scikit-learn (metrics recompute), nbformat/nbconvert for the notebook PDF.

---

### Task 1: Add best/average/worst helper

**Files:**
- Modify: `docs/builders/build_final_report.py`

- [ ] Add a small helper near the top of `build_text()`:

```python
def bucket_stats(rows):
    """rows: list of (label, f1). Returns (best_label, best_f1, worst_label, worst_f1, mean_f1)."""
    best = max(rows, key=lambda r: r[1])
    worst = min(rows, key=lambda r: r[1])
    mean = sum(f for _, f in rows) / len(rows)
    return best[0], best[1], worst[0], worst[1], mean
```

- [ ] Commit: `git add docs/builders/build_final_report.py && git commit -m "report: add best/avg/worst helper"`

### Task 2: Section 2 — Midterm Result Analysis, best/avg/worst across all 6 classical rows

**Files:** Modify `docs/builders/build_final_report.py`, function `build_text()`, key `T['midterm']`.

- [ ] Build the 6-row list per dataset from `R['classical']` (NB/LR/SVM × BoW/TF-IDF is 6 rows, but the spec's "6 models" = the 3 classical models × their pair of representations counted as one row each is 6 — confirm by using all 6 `(name, rep)` combinations, not per-model-only).
- [ ] For each dataset compute `bucket_stats` over the 6 `(f"{name} ({rep})", f1)` pairs.
- [ ] Rewrite `T['midterm']` prose: state the 6-row comparison, name best/average/worst per dataset with the reason (representation sensitivity, independence assumption for Naive Bayes, etc. — reuse existing reasoning already in the current prose, just anchor it to best/avg/worst explicitly instead of only "best vs best").
- [ ] Keep Table 2 (the classical 6-row table) attached to this section as already implemented.
- [ ] Do not touch table-emission code yet (Task 6 handles the rep-column removal, which is Section 6 only — Section 2's table keeps its Representation column).

### Task 3: Section 3 — Selecting the Best BERT model, isolated 8-row table + best/avg/worst

**Files:** Modify `docs/builders/build_final_report.py`.

- [ ] Add a new table-emission block (near where Table 3 currently is built) that emits **BERT only**, 8 rows × D1/D2 columns — i.e. slice `R['grid']` for `mk == 'BERT'` only, drop the DeBERTa and ENSEMBLE rows from this table.
- [ ] Compute `bucket_stats` over the 8 `(lr,bs,wd)` configs per dataset (16 (D1+D2) F1 values total, but report best/avg/worst per dataset separately since the two datasets are not comparable).
- [ ] Rewrite `T['bert']` to state which configuration is best, which is worst, the mean, and *why* the best config wins (tie to existing "spread across the grid is small" observation already in the prose — keep that finding, just make it explicit best/avg/worst language).
- [ ] Table 3 (the combined 16-row BERT+DeBERTa+ENSEMBLE grid) is removed from Section 3; Section 3 gets only its own 8-row BERT table.

### Task 4: Section 4 — Selecting the Best BERT variant (DeBERTa) model, isolated 8-row table + best/avg/worst + sir's-5-configs comparison

**Files:** Modify `docs/builders/build_final_report.py`.

- [ ] Update the section-4 heading text passed to the docx (the template placeholder itself can't be renamed without editing the `.docx` template — instead the inserted prose's first line must explicitly name "DeBERTa (BERT variant)" so the variant model name is visible even though the template heading says only "4 Selecting the Best BERT variant model"). Confirm whether the template heading paragraph itself should be edited via `find_par` + `set_text` to append "— DeBERTa" (do this: `set_text(find_par(doc,'4 Selecting the Best BERT variant model'), '4 Selecting the Best BERT variant model — DeBERTa', ...)`).
- [ ] Add a DeBERTa-only 8-row table (same pattern as Task 3, `mk == 'DeBERTa'`).
- [ ] Compute `bucket_stats` over DeBERTa's 8 configs per dataset, rewrite `T['variant']` to state best/avg/worst with reason.
- [ ] **Blocked sub-step (do not fabricate):** leave a clearly marked one-paragraph placeholder in `T['variant']` — `"[PENDING: sir's 5 specified configurations vs. our 8-point grid — insert once configs are provided]"` — so it's impossible to mistake for finished prose, and note this in the plan's final status, not in the shipped docx. Do not run the final docx build with this placeholder still in place; block Task 10 until resolved or explicitly told to ship without it.

### Task 5: Section 5 — Ensemble Model, isolated table + naming + why

**Files:** Modify `docs/builders/build_final_report.py`.

- [ ] Replace the ensemble table currently folded into Table 3's last row with its own small table under Section 5: 2 rows (D1, D2), columns Weight-on-BERT, Acc, Prec, Rec, F1.
- [ ] Rewrite `T['ensemble']` first line to explicitly name both member models ("BERT and DeBERTa (BERT variant)") in the heading sentence.
- [ ] Keep the existing "why it didn't help" mechanism explanation (McNemar p-value, degenerate weight) — that content is already correct and should not be regenerated from scratch, only relocated/re-anchored to the new isolated table.

### Task 6: Section 6 — Overall Result Analysis, sir's two tables + 6-row best/avg/worst, drop Rep column

**Files:** Modify `docs/builders/build_final_report.py`.

- [ ] Table A = the full sweep table (currently Table 3, 17 rows incl. ensemble) — move its emission to be anchored under Section 6 instead of Section 3.
- [ ] Table B = the final 6-row comparison (currently Table 5) — modify column layout to **remove** the separate "Representation" column and instead append `[BoW]` or `[TF-IDF]` directly onto the classical model name cell, e.g. `"Naive Bayes [BoW]"`. Transformer/Ensemble rows keep plain names (no `[raw]` tag needed since that was only there to fill the Representation column).
- [ ] Compute `bucket_stats` over the final 6 rows (NB/LR/SVM/BERT/DeBERTa/ENSEMBLE, each already at its best representation) per dataset, add prose stating best/average/worst with reason (this is largely the existing `T['overall']` content — reuse it, just make best/avg/worst explicit for the 6-row table specifically, distinct from the per-family analysis already there).
- [ ] Keep the existing 128-token / surface-vs-content analysis paragraphs in `T['overall']` — they are correct findings, not being replaced, only supplemented.

### Task 7: Sections 1, 7, 8 — trim to essentials

**Files:** Modify `docs/builders/build_final_report.py`, keys `T['intro']`, `T['limits']`, `T['conclusion']`.

- [ ] Re-read each paragraph against the template's one-line prompt ("Provide an overall summary... few lines", "Discuss the limitations", "Summarize your work... motivate the audience") and cut sentences that don't serve that prompt directly. Target: shorten `T['intro']` to setup + headline result only (currently includes a full methods paragraph that duplicates Sections 3/4 — that duplication is the "unnecessary" text to remove, per the user's Bangla instruction "OPROJONIYO ICHU LEKHA JABE NA").
- [ ] `T['limits']` and `T['conclusion']`: leave largely as-is (user said "porey dekhio thik ache naki" — review these two after the rest of the rewrite, don't blindly cut) — do a pass only after Tasks 2–6 land, since limitations/conclusion phrasing depends on what the restructured sections above end up saying.

### Task 8: Remove the code-appendix block from the docx build

**Files:** Modify `docs/builders/build_final_report.py`, `main()`.

- [ ] Delete (or `if False:`-guard, then delete once confirmed) the entire `# ---- code appendix` block at the end of `main()` that inserts `code_listings()` into the doc.
- [ ] Leave `code_listings()` function itself defined but unused, or delete it too if nothing else calls it — check with `grep -n code_listings docs/builders/build_final_report.py` before deleting to confirm no other caller.
- [ ] Also remove/blank the four template appendix headings ("Data preprocessing code if there is any", "BERT code...", "BERT Variant code...", "Ensemble code") the same way the current code already strips them (`find_par` + remove) — keep that removal, just don't insert replacement content after it.

### Task 9: Notebook reorganization — clear headed sections, preprocessing exactly matches what runs

**Files:** `notebooks/nlp_final_submission_code.ipynb` (canonical submission notebook per `docs/builders/build_final_report.py`'s own `NB` constant).

- [ ] Read the notebook's cell list (`jupyter nbconvert --to script` or直接 load with `nbformat`) and enumerate current markdown headers vs. code cells.
- [ ] Group cells under exactly these markdown headers, in this order, matching the docx section numbering so a reader can map code to report section 1:1: `Setup & Imports`, `Data Loading & Class Balancing`, `Group-Aware Split`, `Classical Text Preprocessing`, `Classical Models (Midterm)`, `Tokenisation for Transformers`, `Fine-Tuning Harness`, `BERT Sweep`, `DeBERTa Sweep`, `Ensemble`, `Final Comparison / Overall Results`.
- [ ] Audit `Classical Text Preprocessing` cell(s): confirm the code there is byte-identical to what actually produced the numbers in `experiments/audit/` (no dead/unused preprocessing variants left in). Remove any cell that isn't on the path to a saved artefact.
- [ ] Do not add narrative prose duplicating the report — keep markdown headers short (section title only), per user instruction ("just say what part it is, no extra part added").

### Task 10: Export the reorganized notebook to PDF (not into the docx)

**Files:** New output, e.g. `docs/nlp_final_submission_code.pdf`.

- [ ] Run (using the project's `.venv` python from `[[nlp-final-external-paths]]`):
```bash
"/media/filwel/All/Sakib/Semester 10/ NATURAL LANGUAGE PROCESSING /Project /.venv/bin/jupyter" nbconvert --to pdf "notebooks/nlp_final_submission_code.ipynb" --output-dir docs/
```
- [ ] If `nbconvert --to pdf` fails on this machine (no LaTeX), fall back to `--to html` then a headless print-to-PDF (`playwright` or `wkhtmltopdf`), documented as a fallback only if the primary path errors.
- [ ] Confirm output PDF page count is reasonable (has all cells, not truncated) by checking `pdfinfo`.

### Task 11: Rebuild docx, banned-glyph audit, verification

**Files:** `docs/NLP Final Report - Group 02 Section B.docx` (generated, not hand-edited).

- [ ] Run: `"<venv python>" docs/builders/build_final_report.py`
- [ ] Run the mandatory banned-glyph audit (Section-mark, pilcrow, dagger, double-dagger — CLAUDE.md Section 0) against the generated docx text (extract via `docx2txt` or `python-docx` paragraph dump) and against `build_final_report.py` itself.
- [ ] Open the docx (or dump all paragraph/table text) and manually confirm: Section 3 table has exactly 8 BERT rows per dataset (no DeBERTa/ENSEMBLE rows), Section 4 table has exactly 8 DeBERTa rows, Section 5 table has exactly 2 ensemble rows, Section 6 has both the 17-row sweep table and the 6-row final table with no Representation column and `[BoW]`/`[TF-IDF]` tags on classical model names.
- [ ] Do not claim complete until Task 4's blocked sub-step is either resolved (configs pasted in) or the user has explicitly said ship without it.

---

## Status tracking (not part of the template — for this session)

- Blocked: Task 4's sir's-5-configs comparison, pending user pasting the actual configs.
- Everything else in this plan is unblocked and can proceed now.
